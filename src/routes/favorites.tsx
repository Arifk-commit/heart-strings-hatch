import { createFileRoute, Link } from "@tanstack/react-router";
import { Heart, Search } from "lucide-react";
import { PetCard } from "@/components/pawconnect/pet-card";
import { Button } from "@/components/ui/button";
import { useApp } from "@/context/app-context";
import { pets } from "@/data/pets";
export const Route=createFileRoute("/favorites")({head:()=>({meta:[{title:"Your Favorites — PawConnect"},{name:"description",content:"Review pet profiles saved locally on this device."},{property:"og:title",content:"Your Favorites — PawConnect"},{property:"og:description",content:"Your locally saved fictional PawConnect pets."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Favorites});
function Favorites(){const{favorites,hydrated}=useApp();const saved=pets.filter(p=>favorites.includes(p.id));return <main className="page-enter mx-auto min-h-[70vh] max-w-7xl px-5 py-16 lg:px-8"><p className="text-sm font-extrabold uppercase text-teal">Saved on this device</p><h1 className="mt-2 font-display text-5xl font-black">Your Favorites</h1><p className="mt-4 text-muted-foreground">A little shortlist of pets who caught your heart.</p>{hydrated&&saved.length?<div className="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">{saved.map(p=><PetCard key={p.id} pet={p}/>)}</div>:hydrated?<div className="mt-12 rounded-3xl border border-dashed bg-secondary/50 px-5 py-20 text-center"><Heart className="mx-auto size-12 text-primary"/><h2 className="mt-5 font-display text-3xl font-bold">No favorites yet</h2><p className="mx-auto mt-2 max-w-md text-muted-foreground">Tap the heart on any pet to save them here. Favorites remain on this device.</p><Button asChild className="mt-6"><Link to="/pets"><Search/>Find a pet</Link></Button></div>:null}</main>}
